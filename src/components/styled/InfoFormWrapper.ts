import { styled } from "@mui/system";

export const InfoFormWrapper = styled("div")({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  flexWrap: "wrap",
  marginInline: "0.5em",
});
