import { styled } from "@mui/system";

export const InfoFormWrapper = styled("div")({
  display: "flex",
  justifyContent: "flex-start",
  alignItems: "center",
  flexWrap: "wrap",
  margin: "2em 1em",
});
