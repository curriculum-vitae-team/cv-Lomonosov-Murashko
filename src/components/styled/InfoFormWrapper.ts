import { styled } from "@mui/system";

export const InfoFormWrapper = styled("div")({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  flexWrap: "wrap",
  margin: "2em 1em",
});
