import { styled, Stack } from "@mui/material";

export const StyledStack = styled(Stack)({
  position: "absolute",
  bottom: "0",
  right: "0",
  backgroundColor: "#001529",
  color: "#fff",
  width: "100%",
  minHeight: "3em",
});
